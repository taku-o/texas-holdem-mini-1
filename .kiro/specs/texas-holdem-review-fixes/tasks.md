# Implementation Plan: texas-holdem-review-fixes

## Implementation Tasks

- [x] 1. ベッティングのバリデーションとラウンド終了・有効アクション返却の修正
- [x] 1.1 (P) ベット・レイズ時に action.amount がプレイヤー所持チップ以下であることを検証し、違反時は状態を更新せずエラーを返すようにする
  - レイズ額が最低レイズ額以上かつルール上有効な範囲であることも検証する
  - _Requirements: 1_

- [x] 1.2 (P) レイズを選択可能とする条件に「コール額＋最低レイズ額を支払えるか」を追加し、支払えない場合はレイズを有効にしない
  - _Requirements: 1_

- [x] 1.3 (P) ベッティングラウンド終了判定を、last aggressor がオールインでアクティブでない場合でも正しく終了するように変更する
  - 無限ループが発生しない終了条件に修正する
  - _Requirements: 2_

- [x] 1.4 (P) 有効アクション取得時に、ベット/レイズの選択可能な額の範囲（min/max 等）を返すようにし、UI がチップ入力の範囲として利用できるようにする
  - _Requirements: 8_

- [x] 2. ブラインド・ショートスタックの扱いの修正
- [x] 2.1 (P) ビッグブラインドがショートスタックの場合、currentBet に実際にポストした額を設定する（BIG_BLIND 固定にしない）
  - _Requirements: 3_

- [x] 2.2 (P) ブラインドを課す対象を「チップが 0 でないアクティブなプレイヤー」に限定し、脱落済みプレイヤーにはブラインドをポストしない
  - _Requirements: 3_

- [x] 3. ショーダウン時のポット配分の正当性確保
- [x] 3.1 単一ポットの前提で、配分後いかなるプレイヤーのチップも負にならないようにする
  - オールインが複数いる場合の配分ロジックを確認し、必要なら修正する。仕様として単一ポット・簡略化配分であることを明記する
  - _Requirements: 4_

- [x] 4. チップが 0 のプレイヤーを次ハンドから除外する
- [x] 4.1 1 ハンド終了後にチップが 0 のプレイヤーを次のハンドに参加させない（除外またはシットアウトとする）
  - _Requirements: 5_

- [x] 4.2 次ハンドのディーラー・席順・ブラインドの計算で、チップ 0 のプレイヤーをスキップする
  - _Requirements: 5_

- [x] 5. CPU のレイズ額がルール上有効になるようにする
- [x] 5.1 (P) CPU がレイズを選んだ場合、レイズ額が currentBet（またはコールに必要な額）以上となるようにする
  - ショートスタックでオールインする場合も、実質ベット額が有効な最小レイズ以上になるようにする
  - _Requirements: 6_

- [x] 6. 役判定のロイヤルフラッシュをライブラリ内部定数に直接依存しない形にする
- [x] 6.1 (P) ロイヤルフラッシュの判定を、アプリ側で定義した条件または定数で行うようにし、ライブラリの内部数値に直接依存しないようにする
  - _Requirements: 7_

- [ ] 7. ゲーム開始時の乱数関数の結合を契約として明示する
- [ ] 7.1 ゲーム開始時の席決定などに用いる乱数が、引数で渡す形で差し替え可能であることをコメントまたは設計で明記する
  - 既存の randomFn 引数で充足している場合は記載のみでよい
  - _Requirements: 9_

- [ ] 8. CPU 連続処理の非同期化
- [ ] 8.1 CPU の連続ターン処理を、メインスレッドを長時間ブロックしない形に変更する
  - 一定件数ごとに yield する、または非同期で次のティックに回す等で、状態更新・再描画のサイクルが進むようにする
  - 呼び出し側（useGameController）とのインターフェース（同期で 1 回分返すか、Promise で複数回 setState するか）に合わせて実装する
  - _Requirements: 10_

- [ ] 9. ゲーム開始時の setState を関数形式に統一する
- [ ] 9.1 (P) startGame で初期状態を設定する際、前回状態に依存しない形で setState する（例: setState(() => newState)）
  - _Requirements: 14_

- [ ] 10. リスト表示の key を安定した識別子にする
- [ ] 10.1 (P) テーブル上のコミュニティカードの key に、配列 index ではなくカードの一意識別子（例: suit + rank）を使用する
  - _Requirements: 11_

- [ ] 10.2 (P) 席やカードのリストで index を key にしている箇所があれば、プレイヤー ID またはカードの一意 ID に変更する
  - PlayerSeats は既に player.id の場合は変更不要。PlayerSeat 内のカードリストがあれば同様に修正する
  - _Requirements: 11_

- [ ] 11. チップ額入力のクライアント側バリデーションとアクセシビリティ
- [ ] 11.1 ベット/レイズ確定前に、最小・最大・所持チップの範囲内であることをクライアント側で検証し、範囲外の値では送信を無効化するかエラーメッセージを表示する
  - _Requirements: 12_

- [ ] 11.2 チップ額用のスライダーおよび数値入力に、label または aria-label で意味が分かるラベルを付与する
  - _Requirements: 13_

- [ ] 11.3 カードの裏面表示に、alt テキストまたは aria-label で「伏せたカード」などアクセシブルな説明を付与する
  - _Requirements: 13_

- [ ] 12. 結合・動作確認とテスト
- [ ] 12.1 上記修正後、ゲーム開始から 1 ハンド進行・人間のアクション・CPU の自動行動・ショートスタック・オールイン・チップ 0 除外・次ハンドまでが意図どおり動作することを確認する
  - _Requirements: 1, 2, 3, 4, 5, 6_

- [ ] 12.2 ベッティングのバリデーション・ラウンド終了判定・ブラインド・ショーダウン・handProgression・cpuStrategy・handEvaluator の単体テストを追加または更新し、修正内容が仕様を満たすことを検証する
  - _Requirements: 1, 2, 3, 4, 5, 6, 7_

- [ ] 12.3* 必要に応じて、ActionBar のチップ範囲検証やアクセシビリティ（ラベル・alt）に関する UI テストを追加する
  - _Requirements: 12, 13_
