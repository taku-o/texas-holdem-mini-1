# 決定ログ

## 1. 新規テスト追加は不要と判断
- **背景**: 既存テストが `player.isHuman` ベースの振る舞い（カード表面表示、ハイライト等）を十分にカバーしている。`createFivePlayers()` は既に `isHuman: i === 0` を設定しており、テストロジックの変更は不要
- **検討した選択肢**: (A) 新規テストケース追加 (B) 既存テストの prop 修正のみ
- **理由**: 今回はインターフェース変更（prop 削除）であり、振る舞いの変更はない。既存テストの prop を修正するだけで、実装後に全テストがパスすることを確認できる

## 2. data-testid クエリの維持
- **背景**: ラッパー div 除去後も `data-testid` は PlayerSeat のルート div に移動するため、`container.querySelectorAll('[data-testid^="player-seat-"]')` のクエリは引き続き機能する
- **検討した選択肢**: (A) クエリ方法を変更 (B) 既存クエリを維持
- **理由**: DOM 構造は変わるが、`data-testid` の値と配置先は同じセマンティクスを持つため、テストコードの変更は不要
