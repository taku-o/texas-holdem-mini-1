# 決定ログ

## 1. テスト追加不要と判断
- **背景**: タスク3（ショーダウン時のポット配分の正当性確保）に対するテスト作成が必要か検討した
- **検討した選択肢**:
  - A) 新規テストを追加する
  - B) 既存テストで十分と判断し、追加しない
- **理由**: `src/domain/showdown.test.ts` に以下のテストが既に存在し、タスク3の全要件をカバーしている:
  - `evaluateShowdown ポット配分の正当性`: オールイン複数(chips=0)、chips=0の勝者へのpot配分、pot=0のエッジケース
  - `resolveUncontestedPot ポット配分の正当性`: chips=0の勝者、pot=0
  - `evaluateShowdown エッジケース`: 3-way/4-way タイの端数処理、奇数ポット
  - チップ保存則（`calcTotalChips`）の検証が複数テストで実施済み
  - 配分ロジックは加算のみで減算操作がなく、chips が負になる経路は構造的に存在しない
