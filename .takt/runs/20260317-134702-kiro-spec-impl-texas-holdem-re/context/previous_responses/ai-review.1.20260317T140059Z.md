All claims have been verified against the actual source code. Here is my review:

---

## AI Antipattern Review — タスク3: ショーダウン時のポット配分の正当性確保

### 判定: **APPROVE** ✅

### レビュー対象

タスク3（要件4: ショーダウン・ポット配分）の実装。implement ムーブメントが「プロダクションコード・テストともに変更不要」と判断し、変更差分は0行。

### ファクトチェック結果

implement ムーブメントの主張をすべて実コードで検証した。

| 主張 | 検証結果 |
|------|---------|
| `showdown.ts:28-30` に仕様コメントが既存 | ✅ 確認済み。「本バージョンではサイドポットを実装せず、単一ポットで均等配分する」「端数は最初の勝者に加算」「不変条件: chips >= 0 かつ配分合計 = pot」 |
| `showdown.ts:46-47` に `resolveUncontestedPot` の仕様コメントが既存 | ✅ 確認済み。「全員フォールドで1人残った場合の単一ポット配分」「不変条件: 配分後、全プレイヤーの chips >= 0」 |
| 配分ロジックは加算のみ（`p.chips + share + extra`） | ✅ `showdown.ts:39` で確認。`share = Math.floor(pot / winners.length)` は非負、`extra = remainder` は非負。減算操作なし |
| `resolveUncontestedPot` も加算のみ | ✅ `showdown.ts:52` で `p.chips + state.pot` と確認 |
| テスト16件がパス（492行） | ✅ `showdown.test.ts` で16テストケースを確認。chips=0, pot=0, 端数, 保存則, 不変性すべてカバー |
| `calcTotalChips` による保存則チェックが複数テストで実施 | ✅ `showdown.test.ts` の行240-241, 301-302, 334-335, 466-467 で確認 |

### AI アンチパターン検出結果

| 観点 | 結果 |
|------|------|
| 仮定の妥当性 | ✅ 問題なし。「変更不要」の判断は構造的安全性（加算のみ）と既存テストカバレッジの双方を根拠としており、妥当 |
| 幻覚API・存在しないメソッド | ✅ 該当なし（コード変更なし） |
| 既存コードベースとの整合性 | ✅ 該当なし（コード変更なし） |
| スコープクリープ | ✅ 問題なし。不要な防御的コード（`assert(chips >= 0)` 等）を追加しなかった判断は適切。AIにありがちな「念のため」コード追加を正しく回避 |
| スコープ縮小（要件の取りこぼし） | ✅ 問題なし。要件4の受け入れ基準「単一ポットの簡略化仕様の範囲で、不正なマイナスチップ等が発生しない」は、既存の構造で充足されている |
| デッドコード・未使用コード | ✅ 該当なし（コード変更なし） |
| フォールバック・デフォルト引数の濫用 | ✅ 該当なし（コード変更なし） |
| 決定トレーサビリティ | ✅ `coder-decisions.md` に選択肢A（防御的チェック追加）とB（変更しない）が記録され、Bの選択理由が具体的に説明されている |

### 総合評価

変更差分が0行のため、ボーイスカウトルールの適用対象ファイルも存在しない。implement ムーブメントの「変更不要」判断は、以下の根拠に基づいており正当：

1. **構造的安全性**: 配分ロジックが加算のみで構成されており、chips が負になる数学的経路がない
2. **仕様の明記**: 単一ポット・簡略化配分の方針がコメントで既に文書化済み
3. **テストカバレッジ**: エッジケース（chips=0, pot=0, 端数, 3-way/4-way タイ）がすべてテスト済み
4. **チップ保存則**: `calcTotalChips` による検証が複数テストに含まれている

AIにありがちな「念のため」の防御的コード追加（到達不能なアサーション等）を正しく回避した点は、むしろAIアンチパターンの回避として評価できる。

**ブロッキング問題: 0件 / 非ブロッキング問題: 0件**