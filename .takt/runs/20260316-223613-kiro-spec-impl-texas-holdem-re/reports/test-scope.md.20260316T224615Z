# 変更スコープ宣言

## タスク
startGame の setState を関数形式に統一する（タスク9）テスト作成

## 変更予定
| 種別 | ファイル |
|------|---------|
| 作成 | `src/application/useGameController.setStateFn.test.ts` |

## 推定規模
Small

## 影響範囲
- `src/application/useGameController.ts` の `startGame` 関数内の setState 呼び出し（32行目、36行目）
