# 決定ログ

## 1. テスト追加不要と判断
- **背景**: タスク7はJSDocコメント追加のみで、振る舞い変更がないため、新規テストの要否を判断する必要があった
- **検討した選択肢**: (A) JSDocの存在を検証するテストを追加 (B) テスト追加なし
- **理由**: JSDocコメントはランタイムの振る舞いに影響せず、テストで検証すべき対象ではない。既存テストが `randomFn` の依存注入パターンを十分にカバーしており、計画レポートも「テストの追加も不要」と明記しているため、(B) を選択
