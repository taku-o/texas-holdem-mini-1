調査完了。分析結果をまとめます。

---

## 計画レポート: タスク7 — ゲーム開始時の乱数関数の結合を契約として明示する

### 要件分析

**対象要件:** Requirement 9（乱数関数の結合）

> ゲーム開始時の席決定などで用いる乱数生成を、差し替え可能な形（引数で渡す、または依存注入）で結合し、テストでは固定シードやモックを注入できるようにする。

**タスク7の指示:**
> ゲーム開始時の席決定などに用いる乱数が、引数で渡す形で差し替え可能であることをコメントまたは設計で明記する。既存の randomFn 引数で充足している場合は記載のみでよい。

---

### 現行実装の調査結果

**変更要否の判定:**

| 観点 | 現行実装 | 判定 |
|------|---------|------|
| `setupNewGame` が `randomFn` を引数で受けている | ✅ `gameSetup.ts:6` — `setupNewGame(randomFn: () => number)` | 変更不要 |
| 席決定で `randomFn` を使用 | ✅ `gameSetup.ts:7-8` — `Math.floor(randomFn() * PLAYER_COUNT)` | 変更不要 |
| デッキシャッフルで `randomFn` を使用 | ✅ `gameSetup.ts:19` — `shuffleDeck(createDeck(), randomFn)` | 変更不要 |
| テストで固定シード/モックを注入可能 | ✅ `gameSetup.test.ts` — 全テストが `() => 0.5` 等の固定関数を渡している | 変更不要 |
| 呼び出し元で `randomFn` を注入 | ✅ `useGameController.ts:15` — `useGameController(randomFn)` → `App.tsx:6` — `Math.random` を渡す | 変更不要 |
| `randomFn` がゲーム進行全体を通じて一貫して渡されている | ✅ `gameFlow.ts` 全関数が `randomFn` を引数で受け取り、`cpuStrategy.ts:87` や `handProgression.ts:75` にも伝播 | 変更不要 |

**結論:** 既存の `randomFn` 引数パターンで要件9は**完全に充足**されている。乱数関数はすべて引数経由で注入されており、`Math.random` の直接呼び出しはドメイン・アプリケーション層に存在しない（`App.tsx` のルートでのみ `Math.random` を渡している）。

---

### 実装アプローチ

タスク指示書に「既存の randomFn 引数で充足している場合は記載のみでよい」と明記されている。

**必要な変更: `gameSetup.ts` の `setupNewGame` 関数に JSDoc コメントを追加**

現在 `setupNewGame` には JSDoc がない。この関数の `randomFn` パラメータが「テスト時の差し替え・再現性確保のための依存注入ポイント」であることを設計意図として明示するコメントを追加する。

**変更ファイル:** `src/domain/gameSetup.ts` のみ（1ファイル）

---

### Coder向け実装ガイドライン

#### 変更内容

`src/domain/gameSetup.ts` の `setupNewGame` 関数（6行目）の直前に JSDoc コメントを追加する。

**コメントに含めるべき内容:**
- `randomFn` が席決定・デッキシャッフル等のゲーム初期化で使用される全乱数のソースであること
- テストでは固定値関数やシード付き乱数を注入して決定論的な結果を得られること（設計判断の Why）

**コメントに含めてはいけない内容（ナレッジ制約）:**
- コードの動作をそのまま言い換える What/How コメント（例: 「randomFn を使ってプレイヤー数から席を決定する」）
- TODO コメント

#### 参照すべき既存実装パターン

他のファイルでの `randomFn` の JSDoc/コメントはないため、新規追加となる。コメントのスタイルは TypeScript の標準的な JSDoc `@param` 記法に従う。

#### 注意すべきアンチパターン

- **説明コメント（What/How）の禁止:** 「randomFn で席を決める」のようなコードの言い換えは書かない。なぜ引数で受け取る設計なのか（テスタビリティ・再現性）という Why を書く
- **空実装・スタブ放置の禁止:** 形式的なコメント（`/** ゲームをセットアップする */`）ではなく、情報を追加するコメントにする

#### 影響範囲

- JSDoc の追加のみで、関数シグネチャ・振る舞い・テストへの影響なし
- 他ファイルへの配線変更なし

#### スコープ

このタスクは「コメントまたは設計で明記する」のみ。実装変更は不要であり、テストの追加も不要（既存テストが `randomFn` の差し替え可能性を十分に証明している）。