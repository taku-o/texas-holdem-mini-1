調査完了しました。以下が計画レポートです。

---

## 計画レポート: タスク1「デバッグ用ファイルの扱い方針の決定と反映」

### 要件の理解

タスク1は2つのサブタスクから構成される:
- **1.1**: デバッグ用ファイルの最終的な配置/削除ポリシーを決める
- **1.2**: リポジトリからデバッグ用ファイルを整理する

要件1の受け入れ基準: 本番想定ブランチに `debug_common.ts`, `debug_common.test.ts`, `debug_game.ts`, `debug_game2.ts`, `debug_game3.ts` を含めないこと。

### 現状調査結果

**対象ファイル（すべてルート直下に存在）:**
- `debug_common.ts` (2275 bytes) — テスト用のベッティングラウンド実行ヘルパー・セレクター関数
- `debug_common.test.ts` (12044 bytes) — 上記のテスト
- `debug_game.ts` (1280 bytes)
- `debug_game2.ts` (1022 bytes)
- `debug_game3.ts` (2187 bytes)

**依存関係:**
- `src/` 内からこれらのファイルをインポートしている箇所: **なし**
- デバッグファイル間の依存: `debug_game*.ts` が `debug_common.ts` をインポート

**ビルド設定:**
- `tsconfig.app.json` の `include` は `"src"` のみ → デバッグファイルはビルド対象外（変更不要）
- `vite.config.ts` にデバッグファイルへの参照なし（変更不要）
- `.gitignore` にデバッグファイルの除外設定なし

### 方針決定

タスク指示書の方針に従い、**削除**を採用する。

理由:
- これらはアドホックなデバッグスクリプトであり、再利用可能な開発ツールではない
- `src/` 内から参照されておらず、削除による影響はゼロ
- `tools/` ディレクトリは存在せず、保持のためだけに新設する理由がない
- レビュー指摘が「マージ前に削除が必要」と明記している

### 実装アプローチ

#### タスク1.1: 方針の明文化

**変更ファイル:** なし（ドキュメント追記のみ）

README または適切な開発ドキュメントに「`debug_*.ts` 系ファイルは本番ブランチに含めない」方針を明文化する。

- **対象**: `README.md` が存在すればそこに追記。なければ確認が必要。
- **内容**: 「デバッグ専用ファイル（`debug_*.ts`）は本番ブランチにコミットしない」旨の1セクションを追加

#### タスク1.2: デバッグファイルの削除

**削除対象（5ファイル）:**
1. `debug_common.ts`
2. `debug_common.test.ts`
3. `debug_game.ts`
4. `debug_game2.ts`
5. `debug_game3.ts`

**確認済み事項:**
- `tsconfig.app.json`: `include: ["src"]` のみ → 変更不要
- `tsconfig.node.json`: 未確認だが、デバッグファイルを明示的に含めていない限り影響なし（要確認）
- `vite.config.ts`: デバッグファイルへの参照なし → 変更不要
- `src/` 内: デバッグファイルへのインポートなし → 変更不要

### Coder向け実装ガイドライン

1. **削除対象**: ルート直下の以下5ファイルを `git rm` で削除する
   - `debug_common.ts`
   - `debug_common.test.ts`
   - `debug_game.ts`
   - `debug_game2.ts`
   - `debug_game3.ts`

2. **方針の明文化**: `README.md` にデバッグファイルの運用ルールを1セクション追記する。内容例:
   ```
   ## 開発ルール
   - `debug_*.ts` などのデバッグ専用ファイルは本番ブランチ（main/master）にコミットしない。
   ```

3. **ビルド設定の確認**: `tsconfig.node.json` を読み、デバッグファイルへの参照がないことを確認する。参照があれば除去する。

4. **影響範囲**: `src/` 内からのインポートはゼロであるため、ファイル削除のみで完結する。配線変更は不要。

5. **注意すべきアンチパターン**: 特になし。本タスクは単純なファイル削除とドキュメント追記のみ。

### スコープ外

- `.gitignore` への `debug_*.ts` 追加は不要（ファイル自体を削除するため）
- CI での自動チェック導入はタスク指示書に明記されていないため含めない

### 小規模タスク判定

本タスクは1-2ファイルの変更（実質はファイル削除 + README追記）のみで、設計判断・技術選定は不要。**設計セクションは省略**。