## 作業結果
- タスク1「デバッグ用ファイルの扱い方針の決定と反映」に対する構造テストを作成した
- テストは3ケースで構成:
  1. **指定された5つのデバッグファイルがプロジェクトルートに存在しないこと**を検証（現在は失敗 → 実装で削除後にパス予定）
  2. **`debug_*.ts` パターンに一致するファイルがプロジェクトルートに存在しないこと**を検証（現在は失敗 → 実装で削除後にパス予定）
  3. **`src/` 内からデバッグファイルへのインポート参照がないこと**を検証（現在もパス）
- インテグレーションテストの要否判断: モジュール横断のデータフローなし、状態遷移なし、オプション伝搬なし → **不要**

## 変更内容
- 作成: `src/project-structure.test.ts` — プロジェクト構造の整合性を検証するテスト（3テストケース）